'use client'

import { Flight } from "@/interfaces/Flight"
import { Box } from "@chakra-ui/react"

export default function FlightTab(FlightData : Flight) {
    console.log(FlightData)

    return (
        <Box>
            
        </Box>
    )
}